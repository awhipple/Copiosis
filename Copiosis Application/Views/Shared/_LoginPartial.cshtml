﻿@if (Request.IsAuthenticated)
{
    <li>@Html.ActionLink("Transaction Overview", "Overview", "Account")</li>
    <li>@Html.ActionLink("Item Management", "Items", "Account")</li>
    <li>@Html.ActionLink("Create Item", "AddItem", "Account")</li>
    <li>@Html.ActionLink("I Benefited Someone", "Create", "Account", new { type="producer" }, null)</li>
    <li>@Html.ActionLink("I Received Benefit", "Create", "Account", new { type = "consumer" }, null)</li>
    <li class="last-item"><a href="javascript:document.getElementById('logoutForm').submit()">Log off</a></li>
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm" }))
    {
        @Html.AntiForgeryToken()
    }
}
else
{
    <li>@Html.ActionLink("Register", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
    <li class="last-item">@Html.ActionLink("Log in", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
}
